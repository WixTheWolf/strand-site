# Shared live scoring — either native Upstash names or Vercel KV aliases work.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# KV_REST_API_URL=
# KV_REST_API_TOKEN=

# Server-only captain control credential. Never prefix this with NEXT_PUBLIC_.
STRAND_SCORING_ADMIN_PIN=

# Optional server-side account links. Never commit the populated JSON value.
PLAYER_DATA_LINKS_JSON=
