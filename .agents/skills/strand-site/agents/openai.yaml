interface:
  display_name: "Strand Site"
  short_description: "Repo-specific patterns and workflows for strand-site"
  default_prompt: "Use the strand-site repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true